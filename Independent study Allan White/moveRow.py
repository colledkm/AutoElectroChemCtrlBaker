def moverow(horizontaldist,horizontalDir):

# -------------------------------------------------------------------------
# TODO: Move up
# -------------------------------------------------------------------------

    from moveDist import movedist
    movedist(distance=, direction=,speed=) #move up off of the plate

# -------------------------------------------------------------------------
# TODO: Move to next node, make sure horizontalDirection is compatiable with -1 or 1
# -------------------------------------------------------------------------

    trueHorzDir = horizontalDir #convert -1 or 1 to direction
    movedist(distance=horizontaldist, direction=trueHorzDir,speed=)

# -------------------------------------------------------------------------
# TODO: Move down
# -------------------------------------------------------------------------
    from moveSense import movesense
    movesense()

    return;

